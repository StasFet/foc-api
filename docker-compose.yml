version: "3.9"
services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - CGO_ENABLED=0
    develop:
      watch:
        - path: .
          action: restart
    volumes:
      - ./database:/app/database/